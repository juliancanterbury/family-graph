# Family Graph Photo Cards + Canvas Zoom

Adds:
- Graph cards use the face crop from the first tagged photo as a temporary profile image
- People page also uses the same temporary face image
- Graph zoom controls scale only the graph canvas
- Header/menu text remains the same size
- Relationship validation/review retained

Commit message:
Add face-crop profile placeholders and canvas zoom
