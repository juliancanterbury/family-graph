import { S, $, esc, fullName, titleCaseName, userId, canDelete } from './state.js';
import { avatarHtml } from './render.js';
import { renderAll } from './render.js';
export async function renderPeople(){const list=$('peopleList'); if(!list)return; let out=''; for(const p of S.people){out+=`<div class="people-card">${await avatarHtml(p)}<strong>${esc(fullName(p))}</strong><p>${esc(p.birth_date||'No dates yet')}${p.death_date?' – '+esc(p.death_date):''}</p><button class="danger small-btn owner-only" data-delete-person="${p.id}">Delete</button></div>`} list.innerHTML=out||'<p>No people yet.</p>'}
export async function addPerson(){const raw=prompt('Full name'); const name=titleCaseName(raw); if(!name)return; const parts=name.split(' '); const ins=await S.sb.from('people').insert({display_name:name,given_names:parts[0]||name,family_name:parts.slice(1).join(' ')||null,created_by:userId()}).select().single(); if(ins.error)return alert(ins.error.message); S.people.push(ins.data); await renderAll()}
export async function deletePerson(id){if(!canDelete())return alert('Only owner can delete people.'); const p=S.people.find(x=>x.id===id); if(!p||!confirm(`Delete ${fullName(p)}?`))return; await S.sb.from('relationships').delete().or(`from_person_id.eq.${id},to_person_id.eq.${id}`); await S.sb.from('faces').update({person_id:null,label:null,status:'unconfirmed'}).eq('person_id',id); const del=await S.sb.from('people').delete().eq('id',id); if(del.error)return alert(del.error.message); S.people=S.people.filter(x=>x.id!==id); S.relationships=S.relationships.filter(r=>r.from_person_id!==id&&r.to_person_id!==id); S.faces.forEach(f=>{if(f.person_id===id){f.person_id=null;f.label=null}}); await renderAll()}
export function bindPeople(){document.getElementById('addPersonBtn')?.addEventListener('click',addPerson); document.body.addEventListener('click',e=>{const b=e.target.closest('[data-delete-person]'); if(b)deletePerson(b.dataset.deletePerson)})}
