# Family Graph - Next Stage

Adds the first real feature page:

- `photo-builder.html`
- face naming overlay
- relationship builder
- live mini-tree
- JSON export

Copy these files into the root of your `family-graph` repository, replacing existing `index.html`, `style.css`, and `app.js`.

Then commit:

`Add build tree from photo workflow`
